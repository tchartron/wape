import editor from 'Editor/editor'

// export default {
//     init(options) {
//         editor.init(options)
//     }
// }

// export default function(options) {
//     editor.init(options)
// }
function we(options) {
    editor.init(options)
}
export default we
